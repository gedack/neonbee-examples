# Log files
*.log
*.log.[0-9]*

# the EDMX model files are generated by the npm run compile build step
#**/*.edmx
# the CSN model files are generated by the npm run compile build step
#**/*.csn

# we are using yarn, so never commit package-lock.json
package-lock.json

# Gradle
.gradle/
build/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# Other
.vscode/
node_modules/
.settings/
dist/
.idea/
.project
/.settings/
.classpath
/.checkstyle
/.eclipse-pmd
/.vertx/
/.bin/
/out/
.project
/.vscode/launch.json
*.hprof
.pmd
.idea
.iml
*.tgz